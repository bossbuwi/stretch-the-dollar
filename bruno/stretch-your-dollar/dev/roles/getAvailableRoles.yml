info:
  name: getAvailableRoles
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:{{port}}/role/index
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
