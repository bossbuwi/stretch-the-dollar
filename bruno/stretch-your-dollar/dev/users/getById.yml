info:
  name: getById
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:{{port}}/user/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
