info:
  name: getByUsername
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:{{port}}/user/username?username=admin
  params:
    - name: username
      value: admin
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
